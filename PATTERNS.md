# 言語によらない考え方のコツ

解くたびに追記する。TypeScript 固有の話は [typescript/NOTES.md](typescript/NOTES.md)。
書式は「サイン / 効く理由 / 使えない条件 / 例」の 4 行。

## まずオーダーを見積もる

- **サイン**: 制約に 10^5 以上がある
- **効く理由**: 2 秒の目安は 10^8〜10^9 回。書く前に方針の可否が分かる
- **使えない条件**: なし
- **例**: ABC470 C（N×Q = 2.5×10^11 で 100 倍オーバー）

| N          | 通る計算量          | 手法                 |
| ---------- | ------------------- | -------------------- |
| 10^5〜10^6 | O(N), O(N log N)    | 1 周、ソート、累積和 |
| 10^3       | O(N²)               | 二重ループ、DP       |
| 20 前後    | O(2^N)              | bit 全探索           |

## 全部作り直さない（差分更新）

- **サイン**: クエリのたびに配列全体を集計し直している
- **効く理由**: 「今の答え」を変数で持ち回り、旧値を打ち消して新値を入れれば O(1)。総和は `+= 新-旧`、XOR は `^= 旧^新`
- **使えない条件**: 最大値（消えた値の次点が分からない）
- **例**: ABC470 C、ABC472 C（直近 M 日の合計。窓から出た日を引き、入った日を足す）

区間の合計を聞かれたら反射で 2 択。**区間が少しずつ動く**なら差分更新（しゃくとり法）、
**あとから任意の区間を何度も聞かれる**なら累積和。

## 変わるものだけ持つ

- **サイン**: ループ内の if がほとんど false
- **効く理由**: 条件を満たす添字だけ集合に入れ、そこだけなめる
- **使えない条件**: 毎回大半が集合に出入りする場合
- **例**: ABC470 C（`A_i >= 1` の添字だけ持つ）

## 1 回の最悪でなく合計で見積もる（ならし計算量）

- **サイン**: 「1 回で最大 N 個処理しうる」で諦めそうになる
- **効く理由**: 「減らせるのは過去に増やしたぶんだけ」のように総量を押さえられれば、1 回が重くても平気
- **使えない条件**: 処理 1 回と過去の 1 単位が対応しないとき
- **例**: ABC470 C（合計 O(N+Q)）

## 動かすものを変える（相対化）

- **サイン**: 「全要素に同じ操作」のクエリがある
- **効く理由**: 全員を動かす代わりに基準を動かす。「全員 +1」＝「基準 -1」
- **使えない条件**: 操作が一部の要素だけに効くとき
- **例**: ABC470 C 別解（クエリ 2 の回数 m を持ち、実際の値を `A_i - m` と読む）

## 出力はまとめて 1 回

- **サイン**: ループ内で毎回 print している
- **効く理由**: 出力ごとにシステムコールが発生する。50 万回で数百 ms
- **使えない条件**: 対話型問題（毎回 flush が要る）
- **例**: ABC470 C

## ループの中の「隠れループ」を数える

- **サイン**: for の中に `slice` / `reduce` / `filter` / `includes` / `indexOf` / `sort` がある
- **効く理由**: 1 行に見えて中で回っている。外側の N と掛け算になり O(N²) に化ける
- **使えない条件**: なめる長さが定数で抑えられるとき
- **例**: ABC472 C（毎日 `slice` + `reduce` で窓を数え直して O(NM)。手元で 24 秒）

計算量は「行数」ではなく「触る要素の延べ数」で数える。1 日あたり最大 M 要素触るなら、
その 1 行が O(M)。

## 提出前に最悪ケースを流す

- **サイン**: サンプルが全部 AC で満足しかけている
- **効く理由**: サンプルは小さいので遅さが見えない。制約の最大値で測れば TLE は提出前に分かる
- **使えない条件**: なし（最悪ケースが自明でなくても、N を最大にするだけで大半は暴ける）
- **例**: ABC472 C（`./scripts/bench.sh atcoder/abc472/c` で 23.87 秒 / 制限 2 秒）

N を 2 倍にして時間が **4 倍なら O(N²)、2 倍なら O(N)**。この増え方を見れば、
オーダーの見積もりが合っていたかを実測で答え合わせできる。

## 従属する変数はループから消す

- **サイン**: 三重ループになり、制約を入れると 10^9 回を超える
- **効く理由**: 「合計 N 枚」のような制約があると、他が決まれば残り 1 個は引き算で決まる。ループが 1 本減って O(N³) → O(N²)
- **使えない条件**: 変数どうしを結ぶ等式がないとき
- **例**: ABC085 C（a と b を決めれば c = N - a - b。2000² = 4×10⁶ で 0.06 秒）

## 個数に上限があると貪欲は崩れる

- **サイン**: 「大きい方から詰める」で解こうとしている
- **効く理由**: 貪欲が正しいのは、先に取った選択が後の選択を狭めないとき。「合計 N 枚」のような別軸の制約が入ると、金額は合っても枚数が合わない解に落ちる
- **使えない条件**: 制約が 1 軸だけなら貪欲でよい（両替の最小枚数など）
- **例**: ABC085 C（45000 円を大きい方から詰めると 4 枚。9 枚という条件を満たせない）

## 並べ替えの可否は不変量で照合する

- **サイン**: 「この順番はありうるか」を、実際に手順を再現して確かめようとしている
- **効く理由**: 動かし方に制限があると「i 番目に来る要素が満たすべき性質」が先に決まる。手順を回さず、位置 i と値 P_i の性質を突き合わせるだけで O(N) になる
- **使えない条件**: 位置から性質が一意に定まらないとき（選択の自由度が後に影響する）
- **例**: ABC474 B（10 人ずつのグループは追い越せない → `floor((P_i-1)/10) === floor(i/10)` を全 i で確認）

## 効くのは最後の 1 回だけ（操作を逆から見る）

- **サイン**: 「末尾に移す」「先頭に出す」ような操作が Q 回あり、同じ対象が何度も出てくる
- **効く理由**: 同じ対象への操作は最後の 1 回で上書きされる。各値の「最後に操作された時刻」を配列に持てば、その昇順に並べるだけで結果が決まり、シミュレーションが消えて O(N+Q)
- **使えない条件**: 操作が上書きでなく累積するとき（加算・スワップなど、前の操作の結果が残る）
- **例**: ABC474 C（一度も操作されない値は元の順で先頭、操作された値は最終操作時刻の順で末尾）

## 構築は「不可能を切る → 極端な 1 点で作る」

- **サイン**: 「条件を満たす列が存在するか判定し、存在するなら 1 つ構成せよ」
- **効く理由**: 答えは 1 つ出せばいいので、最適化は要らない。まず不可能条件を式で言い切り、残りは「1 箇所だけ極端、他は最小」のような雑な構成で押し切れることが多い
- **使えない条件**: 総和や個数に上限があり、極端な値を置けないとき
- **例**: ABC474 D（差を `Σ(A_j−B_j)W_j` にまとめる → 全 j で `A_j≤B_j` なら No。1 つでも `A_i>B_i` があれば `W_i` だけ巨大、他は 1）

「1 つでも成り立てば Yes」の形になったら、**最初に見つけた 1 つで即 break** してよい。
