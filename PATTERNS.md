# 言語によらない考え方のコツ

解くたびに追記する。TypeScript 固有の話は [typescript/NOTES.md](typescript/NOTES.md)。
書式は「サイン / 効く理由 / 使えない条件 / 例」の 4 行。

## まずオーダーを見積もる

- **サイン**: 制約に 10^5 以上がある
- **効く理由**: 2 秒の目安は 10^8〜10^9 回。書く前に方針の可否が分かる
- **使えない条件**: なし
- **例**: ABC470 C（N×Q = 2.5×10^11 で 100 倍オーバー）

| N          | 通る計算量          | 手法                 |
| ---------- | ------------------- | -------------------- |
| 10^5〜10^6 | O(N), O(N log N)    | 1 周、ソート、累積和 |
| 10^3       | O(N²)               | 二重ループ、DP       |
| 20 前後    | O(2^N)              | bit 全探索           |

## 全部作り直さない（差分更新）

- **サイン**: クエリのたびに配列全体を集計し直している
- **効く理由**: 「今の答え」を変数で持ち回り、旧値を打ち消して新値を入れれば O(1)。総和は `+= 新-旧`、XOR は `^= 旧^新`
- **使えない条件**: 最大値（消えた値の次点が分からない）
- **例**: ABC470 C

## 変わるものだけ持つ

- **サイン**: ループ内の if がほとんど false
- **効く理由**: 条件を満たす添字だけ集合に入れ、そこだけなめる
- **使えない条件**: 毎回大半が集合に出入りする場合
- **例**: ABC470 C（`A_i >= 1` の添字だけ持つ）

## 1 回の最悪でなく合計で見積もる（ならし計算量）

- **サイン**: 「1 回で最大 N 個処理しうる」で諦めそうになる
- **効く理由**: 「減らせるのは過去に増やしたぶんだけ」のように総量を押さえられれば、1 回が重くても平気
- **使えない条件**: 処理 1 回と過去の 1 単位が対応しないとき
- **例**: ABC470 C（合計 O(N+Q)）

## 動かすものを変える（相対化）

- **サイン**: 「全要素に同じ操作」のクエリがある
- **効く理由**: 全員を動かす代わりに基準を動かす。「全員 +1」＝「基準 -1」
- **使えない条件**: 操作が一部の要素だけに効くとき
- **例**: ABC470 C 別解（クエリ 2 の回数 m を持ち、実際の値を `A_i - m` と読む）

## 出力はまとめて 1 回

- **サイン**: ループ内で毎回 print している
- **効く理由**: 出力ごとにシステムコールが発生する。50 万回で数百 ms
- **使えない条件**: 対話型問題（毎回 flush が要る）
- **例**: ABC470 C
